'use strict';

/**
 * @file Компонент шапки сайта (навигационная панель)
 * @description Отвечает за отображение верхней навигационной панели с логотипом, поиском, меню, профилем и корзиной
 * @version 1.0.0
 */

// ===================== ИМПОРТЫ (подключение модулей) =====================

/**
 * Импорт Link из React Router DOM
 * @description Компонент для навигации между страницами без перезагрузки страницы
 */
import { Link } from 'react-router-dom'

/**
 * Импорт стилей компонента Header
 * @description CSS-модуль с классами для стилизации шапки
 */
import s from './Header.module.css'


// ===================== КОМПОНЕНТ ШАПКИ САЙТА =====================

/**
 * Компонент Header (шапка сайта)
 * @description Отображает навигационную панель, которая видна на всех страницах приложения.
 * Содержит логотип (ссылка на главную), иконку поиска, меню, профиль пользователя и корзину.
 * 
 * @returns {JSX.Element} JSX-разметка с навигационной панелью
 * 
 * @example
 * // Использование компонента
 * <Header />
 * 
 * // Результат: отображает шапку с логотипом, поиском, меню, профилем и корзиной
 * 
 * @example
 * // Размещение в маршрутизации (рендерится на всех страницах)
 * <BrowserRouter>
 *   <Header />
 *   <Routes>
 *     <Route path="/" element={<HomePage />} />
 *     <Route path="/catalog" element={<Catalog />} />
 *   </Routes>
 * </BrowserRouter>
 */
const Header = () => {
  return (
    // ===================== ОСНОВНОЙ КОНТЕЙНЕР ШАПКИ =====================
    <div className={s.Head}>
      
      {/* ===================== ЛЕВАЯ ЧАСТЬ ШАПКИ ===================== */}
      {/* Содержит логотип (ссылка на главную) и иконку поиска */}
      <div className={s.leftHeader}>
        
        {/* Логотип — клик приводит на главную страницу */}
        <Link to="/">
          <img 
            src="images/logo.png" 
            alt="logo" 
          />
        </Link>
        
        {/* Иконка поиска — открывает поиск товаров (функционал не реализован) */}
        <img 
          src="images/search.png" 
          alt="search" 
        />
      </div>

      {/* ===================== ПРАВАЯ ЧАСТЬ ШАПКИ ===================== */}
      {/* Содержит иконки меню, профиля и корзины */}
      <div className={s.rightHeader}>
        
        {/* Иконка меню — открывает боковое меню (функционал не реализован) */}
        <img 
          src="images/bars.png" 
          alt="menu" 
        />
        
        {/* Иконка профиля — переход в личный кабинет (функционал не реализован) */}
        <img 
          src="images/user.png" 
          alt="user" 
        />
        
        {/* ===================== ССЫЛКА НА КОРЗИНУ ===================== */}
        {/* Иконка корзины — клик приводит на страницу корзины (/basket) */}
        <Link to="/basket">
          <img 
            className={s.cartIcon} 
            src="images/cart.png" 
            alt="Basket" 
          />
        </Link>
      </div>
    </div>
  )
}

// ===================== ЭКСПОРТ КОМПОНЕНТА =====================

/**
 * Экспорт компонента Header по умолчанию
 * @description Делает компонент доступным для импорта в других файлах
 * 
 * @example
 * // Импорт компонента в другом файле
 * import Header from './app/Header/Header'
 */
export default Header